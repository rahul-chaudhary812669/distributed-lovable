spring:
  application:
    name: config-service

  cloud:
    config:
      server:
        git:
          uri: https://github.com/rahul-chaudhary812669/lovable-config-server
          username: rahul-chaudhary812669
          password: ${GIT_PASSWORD}

server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health,info

  health:
    config:
      enabled: false








